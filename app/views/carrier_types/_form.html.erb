<%= form_for(carrier_type) do |f| %>
  <% if carrier_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(carrier_type.errors.count, "error") %> prohibited this carrier_type from being saved:</h2>

      <ul>
      <% carrier_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
