<%= form_for(port) do |f| %>
  <% if port.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(port.errors.count, "error") %> prohibited this port from being saved:</h2>

      <ul>
      <% port.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :port_code %>
    <%= f.text_field :port_code %>
  </div>

  <div class="field">
    <%= f.label :country_id %>
    <!-- <%= f.text_field :country_id %> -->
    <%= f.collection_select :country_id, Country.all, :id, :name %>
  </div>

  <div class="field">
    <%= f.label :port_type_id %>
    <!-- <%= f.text_field :port_type_id %> -->
    <%= f.collection_select :port_type_id, PortType.all, :id, :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
